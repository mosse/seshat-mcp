/**
 * Re-export the forward projection model from the shared package.
 */

export {
  projectForward,
  MODEL_COEFFICIENTS,
  SCALE_COEFFICIENTS,
  HIER_COEFFICIENTS,
  GOV_COEFFICIENTS,
} from '@seshat/shared';
